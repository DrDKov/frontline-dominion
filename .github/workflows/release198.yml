name: Release Frontline Dominion build 198

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build 198 is archived; build 199 is the active release."
