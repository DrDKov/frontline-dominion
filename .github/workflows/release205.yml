name: Legacy Frontline Dominion build 205 publisher (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build 205 publisher disabled; current releases must use the latest build workflow."
