name: Release Frontline Dominion build 209

on:
  push:
    branches: [main, build209-movement-target-fidelity]
    paths:
      - '.github/workflows/release209.yml'
      - 'scripts/assemble209.py'
      - 'src/v209/**'
      - 'tests/free-group-direction209.mjs'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: frontline-release-209
  cancel-in-progress: true

jobs:
  release209:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Publish active release identity
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          rm -rf release209-status
          mkdir release209-status
          cat > release209-status/current.json <<EOF
          {
            "run_id": ${GITHUB_RUN_ID},
            "run_attempt": ${GITHUB_RUN_ATTEMPT},
            "head_sha": "${GITHUB_SHA}",
            "ref": "${GITHUB_REF_NAME}",
            "status": "in_progress",
            "workflow": "${GITHUB_WORKFLOW}",
            "started_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          }
          EOF
          cd release209-status
          git init
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b release209-status
          git add current.json
          git commit -m "Build 209 run ${GITHUB_RUN_ID} started"
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin release209-status

      - name: Assemble build 209
        run: |
          set -euo pipefail
          rm -rf dist
          python scripts/assemble184.py
          python scripts/assemble185.py
          python scripts/assemble186.py
          python scripts/assemble187.py
          python scripts/assemble189.py
          python scripts/finalize189.py
          python scripts/patch189_engineer_parity.py
          python scripts/assemble190.py
          python scripts/install190_runtime.py
          python scripts/assemble191.py
          python scripts/finalize191.py
          python scripts/assemble192.py
          python scripts/finalize192.py
          python scripts/assemble193.py
          python scripts/assemble194.py
          python scripts/assemble195.py
          python scripts/assemble196.py
          python scripts/hotfix196_worker_descriptors.py
          python scripts/hotfix196_worker_scope.py
          python scripts/assemble197.py
          python scripts/hotfix197_worker_action_target.py
          python scripts/hotfix197_input_authority.py
          python scripts/assemble198.py
          python scripts/assemble199.py
          python scripts/hotfix199_runtime.py
          python scripts/assemble200.py
          python scripts/assemble201.py
          python scripts/assemble202.py
          python scripts/assemble203.py
          python scripts/assemble204.py
          python scripts/assemble205.py
          python scripts/assemble206.py
          python scripts/hotfix206_logistics_state.py
          python scripts/hotfix206_truck_commands.py
          python scripts/hotfix206_multiplayer_hash.py
          python scripts/hotfix206_deterministic_geometry.py
          python scripts/hotfix206_multiplayer_initial_state.py
          python scripts/hotfix206_resync_snapshot.py
          python scripts/hotfix206_resync_continuity.py
          python scripts/hotfix206_network_components.py
          python scripts/hotfix206_initial_signature.py
          python scripts/hotfix206_ai_trace_diagnostics.py
          python scripts/install206_ui.py
          python scripts/patch206_invariant_harness.py
          python scripts/assemble207.py
          python scripts/assemble208.py
          python scripts/hotfix208_logistics_replication.py
          python scripts/assemble209.py

      - name: Static build 209 gates
        run: |
          set -euo pipefail
          test -s dist/frontline-dominion.html
          test -s dist/movement-target-fidelity-v209.js
          test -s dist/runtime-ui-v209.js
          test -s dist/runtime-shell-v209.js
          test -s dist/save-slots-v209.js
          grep -q 'data-fd-canonical-build="209"' dist/frontline-dominion.html
          grep -q 'movement-target-fidelity-v209.js?build=209' dist/frontline-dominion.html
          grep -q 'runtime-ui-v209.js?build=209' dist/frontline-dominion.html
          grep -q 'runtime-shell-v209.js?build=209' dist/frontline-dominion.html
          grep -q 'save-slots-v209.js?build=209' dist/frontline-dominion.html
          grep -q 'fd-boot-bridge209' dist/frontline-dominion.html
          grep -q "importScripts('/frontline-dominion/movement-target-fidelity-v209.js?build=209')" dist/authoritative-simulation-worker-v174.js
          grep -q '_fdTranslatedFree209' dist/movement-target-fidelity-v209.js
          python -m py_compile scripts/assemble209.py
          node --check tests/free-group-direction209.mjs
          for file in dist/*.js; do node --check "$file"; done
          count=$(find dist/models -type f | wc -l)
          test "$count" -gt 700

      - name: Install Chromium and generate inherited save gate
        run: |
          set -euo pipefail
          npm install --no-save playwright
          npx playwright install chromium
          python scripts/generate208_tests.py
          node --check tests/save-slots208.generated.mjs

      - name: Physical build 209 gates
        run: |
          set -euo pipefail
          mkdir -p smoke-root
          ln -s "$PWD/dist" smoke-root/frontline-dominion
          python -m http.server 8765 --directory smoke-root >/tmp/fd209-http.log 2>&1 &
          HTTP_PID=$!
          trap 'kill $HTTP_PID 2>/dev/null || true' EXIT
          BASE_URL='http://127.0.0.1:8765/frontline-dominion'
          GAME_URL="$BASE_URL/frontline-dominion.html?build=209"
          ready=0
          for attempt in $(seq 1 50); do
            if curl -fsSL "$GAME_URL" >/tmp/fd209-local.html; then ready=1; break; fi
            sleep 0.1
          done
          test "$ready" -eq 1
          grep -q 'data-fd-canonical-build="209"' /tmp/fd209-local.html
          FD_BASE_URL="$BASE_URL" node tests/logistics206-invariants.mjs 2>&1 | tee /tmp/fd209-invariants.log
          FD_GAME_URL="$GAME_URL" node tests/worker207-bootstrap.mjs 2>&1 | tee /tmp/fd209-bootstrap.log
          FD_GAME_URL="$GAME_URL" node tests/aviation207-worker.mjs 2>&1 | tee /tmp/fd209-air.log
          FD_GAME_URL="$GAME_URL" node tests/free-group-direction209.mjs 2>&1 | tee /tmp/fd209-direction.log
          FD_GAME_URL="$GAME_URL" node tests/logistics208-controls-v2.mjs 2>&1 | tee /tmp/fd209-logistics.log
          FD_GAME_URL="$GAME_URL" node tests/construction208-all-buildings-v2.mjs 2>&1 | tee /tmp/fd209-construction.log
          FD_GAME_URL="$GAME_URL" node tests/save-slots208.generated.mjs 2>&1 | tee /tmp/fd209-save.log

      - name: Publish build 209 to GitHub Pages
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          cd dist
          git init
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b gh-pages
          git add -A
          git commit -m "Frontline Dominion v16.9.3 build 209"
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin gh-pages

      - name: Verify published build 209
        run: |
          set -euo pipefail
          PUBLIC_ROOT='https://drdkov.github.io/frontline-dominion'
          PUBLIC_GAME="$PUBLIC_ROOT/frontline-dominion.html?build=209&verify=${GITHUB_SHA}"
          ok=0
          for attempt in $(seq 1 60); do
            if curl --retry 2 --retry-all-errors -fsSL "$PUBLIC_GAME" >/tmp/fd209-public.html && \
               grep -q 'data-fd-canonical-build="209"' /tmp/fd209-public.html && \
               grep -q 'movement-target-fidelity-v209.js?build=209' /tmp/fd209-public.html && \
               grep -q 'fd-boot-bridge209' /tmp/fd209-public.html; then
              ok=1
              break
            fi
            sleep 5
          done
          test "$ok" -eq 1
          FD_GAME_URL="$PUBLIC_GAME" node tests/free-group-direction209.mjs 2>&1 | tee /tmp/fd209-public-direction.log

      - name: Upload build 209 diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fd209-release-diagnostics
          path: |
            /tmp/fd209-http.log
            /tmp/fd209-local.html
            /tmp/fd209-invariants.log
            /tmp/fd209-bootstrap.log
            /tmp/fd209-air.log
            /tmp/fd209-direction.log
            /tmp/fd209-logistics.log
            /tmp/fd209-construction.log
            /tmp/fd209-save.log
            /tmp/fd209-public.html
            /tmp/fd209-public-direction.log
          if-no-files-found: ignore
          retention-days: 3

      - name: Publish final release result
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          JOB_STATUS: ${{ job.status }}
        run: |
          set -euo pipefail
          rm -rf release209-status
          mkdir release209-status
          cat > release209-status/current.json <<EOF
          {
            "run_id": ${GITHUB_RUN_ID},
            "run_attempt": ${GITHUB_RUN_ATTEMPT},
            "head_sha": "${GITHUB_SHA}",
            "ref": "${GITHUB_REF_NAME}",
            "status": "${JOB_STATUS}",
            "workflow": "${GITHUB_WORKFLOW}",
            "finished_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          }
          EOF
          cd release209-status
          git init
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b release209-status
          git add current.json
          git commit -m "Build 209 run ${GITHUB_RUN_ID} ${JOB_STATUS}"
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin release209-status
